# 제한된 운영 자원에서 실시간 시세 처리량 한계 끌어올리기 Decisions

## D1. 단계별 측정 화면

- 결정일: 2026-09-26
- 검토한 선택지: 통합 대시보드 신규 / 기존 대시보드 + 문서 표 / 쿼리 결과만 문서에
- 선택: **Grafana에 "시세 처리량 부하 시험" 통합 대시보드를 새로 만들고, JSON을 Manifest 레포에 커밋** (사용자 선택)
- 이유: 단계별 전 구간을 한 화면에서 비교할 수 있고, 사용자가 직접 캡처해 포트폴리오 근거로 쓸 수 있다. JSON으로 재현할 수 있다.

## D2. 부하 발생 방식

- 결정일: 2026-09-26
- 검토한 선택지: mock 우선, 막히면 우회 추가 / mock만 / 처음부터 별도 발생 파드
- 선택: **모놀리식 mock으로 시작하고, 모놀리식이 병목이 된 뒤 뒤 단계 측정이 필요할 때만 별도 발생 파드를 추가** (사용자 선택)
- 이유: 실제 시세 진입점(버전 부여 포함)을 파이프라인에 그대로 두고 측정한다. 발생기 자체의 한계는 "목표 대비 실제 발행량"으로 구분한다.

## D3. 증량 단계·유지 시간·중단 기준

- 결정일: 2026-09-26
- 검토한 선택지: 세밀 단계 + 중단 기준 / 거친 단계 + 같은 기준 / 중단 기준 없이 끝까지
- 선택: **세밀 단계 + 중단 기준** (사용자 선택)

### 내용

- 단계(초당 건수): 100 → 250 → 500 → 1,000 → 2,500 → 5,000 → 10,000 → 20,000 → 37,000
- 유지 시간: 단계마다 5분
- 중단 기준(하나라도 해당하면 즉시 이전 단계로):
  - Redis 명령 지연 p99가 50ms를 넘는 상태가 1분 지속
  - 노드 CPU가 95%를 넘는 상태가 1분 지속
  - 파드 재시작 또는 OOM
  - 모놀리식 5xx 비율 1% 초과
- 모놀리식 재기동 없이 mock 설정을 바꾸는 방법을 S1에서 함께 검토한다(재기동마다 약 6분이 든다).

## D4. 자원 재배치 허용 범위

- 결정일: 2026-09-26
- 검토한 선택지: 노드 전체 안에서 자유 조정 / 한도 고정·코드 개선만 / 매번 개별 승인
- 선택: **노드 전체(2대 × 6코어) 안에서 파드 한도·replicas·배치를 자유롭게 조정** (사용자 선택)
- 적용 원칙: 코드 개선과 자원 조정의 효과를 구분하도록 한 번에 하나만 바꾸고, 변경마다 이 문서에 근거와 전후 수치를 남긴다.

### D1 적용 세부 (2026-09-26)

- 대시보드 UID는 `price-throughput-load-test`이다. Grafana API로 생성했다.
- JSON 위치: Manifest 레포의 `monitoring/`은 서비스 Argo CD 애플리케이션이 추적하는지 레포만으로 확인할 수 없었다. 그래서 대시보드 JSON이 매니페스트로 해석될 위험을 피해 **루트 `dashboards/`**에 두었다(사용자 선택. 검토안: 모놀리식 작업 문서 폴더 / Manifest 루트 `dashboards/` / Argo 설정 먼저 확인).
- 패널 구성: ① 부하(목표 대비 발행) ② 모놀리식 실행기·5xx ③ Redis 명령 처리량·상위 명령·지연·증폭률 ④ 워커 소비·반영 지연·미반영 ⑤ 리스너 전달·지연 ⑥ 파드·노드 CPU, 재시작
- 중단 기준 중 "Redis 명령 지연 p99"는 exporter가 명령별 평균 지연만 제공한다. 그래서 워커 쪽 Redis 명령 지연 히스토그램(`profit_worker_redis_command_duration_seconds`) p99로 판정한다.

## D5. mock 부하 조절 방식

- 결정일: 2026-09-26
- 검토한 선택지: 자동 증량 스케줄 + 긴급 정지 키 / Redis 키로 목표 부하 직접 조절 / 관리자 API / 재기동 방식 유지
- 선택: **자동 증량 스케줄 + 긴급 정지 키** (사용자 선택)

### 내용

- mock이 D3 단계표를 스스로 따라 올린다(단계마다 5분). 스케줄은 구독 종목이 처음 생긴 시점부터 시작해, 구독 재구축 시간이 첫 단계를 잡아먹지 않게 한다.
- 목표 부하와 현재 단계를 지표로 노출해 대시보드에서 "목표 대비 실제"를 비교한다.
- Redis 키 `market:mock:control` 하나로 제어한다. mock이 매 틱마다 읽는다.
  - `stop`: 발행 중지
  - `hold`: 현재 단계 고정
  - 숫자: 목표 부하 덮어쓰기
  - 키가 없으면: 스케줄대로 진행
- 이유: 재기동을 한 번만 하고 전 단계를 진행한다. 중단 기준에 걸리면 재기동 없이 즉시 멈출 수 있다.

## D6. 반복 중 운영 반영 방식

- 결정일: 2026-09-26
- 검토한 선택지: 반복마다 main 직접 push / 반복마다 PR 생성·병합
- 선택: **반복마다 작업 브랜치에서 커밋한 뒤 main에 직접 push해 배포·측정** (사용자 선택)
- 적용: 문서(overview~decisions)는 작업 브랜치에 계속 쌓는다. 마지막에 `04-result.md`와 PR을 "이미 반영된 변경의 기록"으로 작성한다.

### D3 정정: 중단 기준 "Redis 명령 지연 p99"의 측정 지표 (2026-09-26 01:26)

- D1 적용 세부에서 워커의 `profit_worker_redis_command_duration_seconds` p99를 중단 기준 지표로 삼았다. 그런데 초당 100건에서 이미 160ms가 나왔다. 확인해 보니 이 값은 명령 하나가 아니라 **명령 수백 개를 묶은 파이프라인 1회의 시간**이었다.
- Redis exporter가 명령별 서버 지연 히스토그램(`redis_commands_latencies_usec_bucket`)을 수집하고 있었다. 관리 명령(`INFO`, `LATENCY` 등)을 빼고 계산하면 서버 명령 p99는 55µs다. 가장 느린 명령은 `SADD` 3.7ms, `EVALSHA` 1.3ms다.
- D3의 기준값(50ms)은 유지하고, 판정 지표만 서버 명령 p99로 바꾼다. 대시보드 패널도 교체했다(워커 파이프라인 1회 시간은 참고선으로 남김).

## S2 1차 증량 측정 (2026-09-26 01:25~01:47)

증량 스케줄(D5)로 100 → 250 → 500 → 1,000 → 2,500(목표)까지 올린 뒤, 01:46에 `stop`으로 중단했다.

| 시각 | 목표 | 실제 발행 | 워커 소비 | 반영 지연 p50/p99 | 모놀리식 CPU | 실행기 큐 | Redis 명령·p99 | 노드 5.104.82.70 |
|---|---|---|---|---|---|---|---|---|
| 01:26 | 100 | 98 | — | —/2.0초 | 0.62/0.7 | — | 19.2만/s·— | 83% |
| 01:30 | 250 | 231 | **157** | 23초/59초 | 0.63 | 16 | 34.7만/s·47µs | **94%** |
| 01:40 | 1,000 | **349** | 158 | 252초/300초+ | 0.57 | **1000/1000** | 34.6만/s·47µs | 94% |
| 01:44 | 2,500 | 412 | 167 | 300초+/300초+ | 0.49 | 1000/1000 | 36.3만/s·47µs | 94% |

### 확인한 병목

1. **워커 처리량이 초당 약 157~169건에서 고정된다.** 워커 CPU는 1.1~1.4/4코어이고 Redis 서버 명령 p99는 47µs로 여유가 있다. 따라서 CPU나 Redis 서버가 아니라 워커 구조(동시성, 파이프라인 왕복, 명령 증폭)나 Redis 왕복 지연에서 막힌다. 발행이 이를 넘으면 반영 지연이 끝없이 늘어난다.
2. **mock 발생기가 초당 약 350~410건에서 포화된다.** `priceEventExecutor` 큐가 가득 차(1000/1000) CallerRuns로 발행 스레드가 막히고, 틱 처리 p99가 110~125ms가 된다. 모놀리식 CPU 한도는 0.7코어다.
3. **운영 Redis(v1 `redis-cluster-0~5`) 6개가 모두 노드 `vmi3244740`에 있다.** 모놀리식까지 같은 노드에 있어 노드 CPU가 94%다. 다른 노드는 44~55%다.

### 참고: redis-cluster-v2

- 사용자가 `redis-cluster-v2`를 두 노드에 분산(4:2)했다. 하지만 운영 앱은 모두 v1(`redis-cluster-*.redis-cluster-headless`)에 연결되어 있다.
- 파드별 CPU(v1 0.26~0.64코어, v2 0.01~0.03코어)와 수신 트래픽(v1 3.3~9.2MB/s, v2 0.01MB/s)으로 v2에 트래픽이 없음을 확인했다. 그래서 이번 부하에는 영향이 없었다.

## D7. 첫 개선 대상

- 결정일: 2026-09-26
- 검토한 선택지: v1 Redis 노드 분산 / 워커 처리량 먼저 / 모놀리식·발생기 먼저
- 선택: **운영 v1 Redis(`redis-cluster`)를 두 노드에 분산** (사용자 선택)
- 이유: 같은 노드의 CPU 경합(94%)이 워커 지연의 원인일 수 있다. 이것을 먼저 없애야 워커 개선 효과를 정확히 잴 수 있다. 마스터·레플리카가 한 노드에 있는 가용성 문제도 함께 해소한다.

### 확인한 제약

- 스테이트풀셋에는 이미 선호형 분산 설정(preferred anti-affinity, `whenUnsatisfiable: ScheduleAnyway`)이 있다. 강제력이 없어 6개가 한 노드에 몰렸다.
- `nodes.conf`와 데이터가 노드에 묶인 PVC(`/data`)에 있다. 파드를 옮기려면 그 파드의 PVC를 지워야 한다. 이때 인스턴스는 새 노드 ID로 빈 채 시작하므로 `CLUSTER FORGET`과 `--cluster add-node --cluster-slave`로 다시 붙여야 한다.
- 매니페스트에서 anti-affinity를 강제형으로 바꾸면 PVC 노드 고정과 충돌해 파드가 `Pending`에 빠질 수 있다. 이 방법은 쓰지 않는다.

### D7 실행 방법 (2026-09-26 01:52)

- 검토한 선택지: 레플리카 이동 + 페일오버 / 앱을 v2로 전환
- 선택: **레플리카 이동 + 페일오버** (사용자 선택)
- v1 마스터–레플리카 짝(01:50 `cluster nodes`):

| 슬롯 | 마스터 | 레플리카 |
|---|---|---|
| 0-5460 | `redis-cluster-4` (431c80…) | `redis-cluster-0` (0e4191…) |
| 5461-10922 | `redis-cluster-1` (bc3d83…) | `redis-cluster-5` (58a4b7…) |
| 10923-16383 | `redis-cluster-3` (a7adaa…) | `redis-cluster-2` (aebe1d…) |

- 절차(파드 하나씩):
  1. `vmi3244740` cordon
  2. 레플리카 PVC·파드 삭제 → `vmi2974623`에 새 볼륨으로 재생성
  3. uncordon
  4. 나머지 노드에서 옛 ID `CLUSTER FORGET`
  5. `--cluster add-node --cluster-slave --cluster-master-id <마스터>`로 재가입
  6. `master_link_status:up` 확인
- 레플리카 0·2·5를 옮긴 뒤 `redis-cluster-0`·`redis-cluster-2`에서 `CLUSTER FAILOVER`를 실행한다. 최종 목표는 `vmi3244740`에 마스터 1 + 레플리카 3·4, `vmi2974623`에 마스터 0·2 + 레플리카 5다.
- 참고: `redis-cluster-0`이 스스로 알리는 IP(`10.42.1.254`)가 파드 IP(`10.42.1.55`)와 다르다. 클러스터가 호스트명으로 통신(`--cluster-preferred-endpoint-type hostname`)하므로 동작에는 영향이 없다.

### D7 실행 결과 (2026-09-26 11:59~12:20, 사용자가 운영 서버에서 실행)

- 레플리카 이동(PVC 삭제 → `vmi2974623`에서 재생성 → `CLUSTER FORGET` → `--cluster add-node --cluster-slave`)
  - `redis-cluster-2`: 11:59:50
  - `redis-cluster-5`: 12:08:36
  - `redis-cluster-0`: 12:15:51
  - 매번 `master_link_up = 1`, 6개 노드 모두 `cluster_state = ok`, known nodes 6개(옛 ID 잔존 없음)를 확인했다.
- `redis-cluster-0`, `redis-cluster-2`에서 `CLUSTER FAILOVER`를 실행했다.
- 최종 배치:

| 노드 | 마스터 | 레플리카 |
|---|---|---|
| `vmi3244740` | `redis-cluster-1` (5461-10922) | 3, 4 |
| `vmi2974623` | `redis-cluster-0` (0-5460), `redis-cluster-2` (10923-16383) | 5 |

- 각 마스터의 레플리카가 다른 노드에 있어 노드 한 대 장애에도 슬롯이 유지된다.
- 매니페스트의 스테이트풀셋 스펙은 바꾸지 않았다(PVC 노드 고정 때문). 새로 생긴 PVC가 `vmi2974623`에 묶여 있어 재시작해도 배치가 유지된다.

### D7 효과 측정 (초당 250건 고정, 2026-09-26 12:21~12:25)

| 지표 | 개선 전 (01:30) | 개선 후 (12:25) |
|---|---|---|
| 노드 CPU (`vmi3244740` / `vmi2974623`) | 94% / 44% | 70% / 78% |
| 워커 소비 | 157건/s | **195건/s (+24%)** |
| Redis 처리 명령 | 34.7만/s | 42.3만/s |
| Redis 서버 명령 p99 | 47µs | 44µs |
| 반영 지연 p50 / p99 | 23초 / 59초 | 17초 / 58초 |
| mock 발행 | 231건/s | 218건/s (모놀리식 CPU 0.67/0.7) |

- 노드 쏠림을 없애자 워커 처리량이 24% 늘었다. 하지만 발행을 따라가지 못해 반영 지연은 계속 쌓인다.
- 다음 병목:
  - Redis 마스터 3대가 각각 초당 약 10만 명령, CPU 0.63~0.68(한도 1), 스로틀링 11~12%다.
  - 시세 1건당 마스터 명령이 약 1,500개다(마스터 합계 약 30만/s ÷ 소비 195건/s). 워커 CPU는 0.73·0.87코어로 스로틀링이 없다. 따라서 워커를 늘리면 Redis 부담만 커진다. 명령 증폭을 줄이는 것이 다음 개선 대상이다.

## 제외 원칙: 종목별 합치기 창 (2026-09-26, 사용자 결정)

- 종목별 합치기 창(같은 종목의 틱을 일정 시간 안에서 최신 1건만 반영)은 **이 작업에서 영구히 제외**한다.
- 이유(사용자): 목표는 초당 처리할 수 있는 트래픽을 재는 것이다. 틱을 합쳐 버리면 처리량이 아니라 버린 양이 늘어날 뿐이다.
- 이 원칙에 따라 기존 동작도 드러낸다.
  - **워커의 배치 내 합치기:** `handleStockMessages`는 배치(최대 100건) 안의 같은 종목을 최신 1건만 반영한다. 버린 건은 `stale_price_event`로 집계된다. 부하가 종목 수(7,384건/s)를 넘으면 사실상 합치기가 된다.
  - **모놀리식 Kafka 발행 필터:** 가격이 직전과 같으면 Kafka로 발행하지 않는다(`lastPublishedPrices`).
- 처리량은 "실제 반영 건수"로 재고, 합쳐서 버린 건수는 따로 표시한다.

## D8. 다음 개선 대상

- 결정일: 2026-09-26
- 검토한 선택지: 유저 총합 증분 갱신 / 레거시 키 제거 / Redis 마스터 3→6 / 현재가 경로 먼저 (종목별 합치기 창은 제외 원칙으로 빠짐)
- 선택: **유저 총합 증분 갱신** (사용자 선택)

## D9. 워커의 배치 내 합치기

- 결정일: 2026-09-26
- 검토한 선택지: 유지하고 분리 집계 / 시험 중 끄기
- 선택: **동작은 유지하고, "반영 건수"와 "합쳐서 버린 건수"를 따로 집계해 처리량은 반영 건수로만 판정** (사용자 선택)

## 진행 순서 변경 (2026-09-26, 사용자 지시)

1. 수익률 경로를 뺀 **현재가 전달 경로(모놀리식 → Redis → Pub/Sub → 리스너 → 웹소켓)**의 한계를 먼저 잰다.
2. 그다음 수익률 경로 부하 시험을 한다.

- 현재가 경로 참고치: 모놀리식 CPU 0.7코어로 초당 약 220건, 스로틀링 49%다. 역산하면 틱당 모놀리식 CPU가 약 3ms다. "초당 수만 건"은 이 틱당 비용을 줄여야 가능하며, 현재 효율로는 불가능하다.

## D10. 현재가 시험 중 수익률 경로 격리

- 결정일: 2026-09-26
- 검토한 선택지: Kafka 발행 끄는 설정 추가 / 워커 파드 0개 / 둘 다 그대로
- 선택: **모놀리식에 "시세 Kafka 발행 on/off" 설정을 추가하고 현재가 시험 중에는 끈다** (사용자 선택)
- 이유: 워커가 Redis CPU를 차지하지 않아 현재가 경로만의 한계를 잴 수 있다. Kafka에 밀림이 쌓이지 않고, 2단계에서 다시 켜기만 하면 된다.

## D11. 현재가 구독자 생성 방식

- 결정일: 2026-09-26
- 검토한 선택지: 로컬 Mac에서 k6 게스트 1,000세션 / 구독자 없이 리스너 수신까지만 / 서버에서 k6 실행
- 선택: **로컬 Mac에서 k6 게스트 세션 1,000개(세션당 20종목)** (사용자 선택)
- 이유: 클러스터 밖에서 돌아 시험 대상 노드의 CPU를 쓰지 않는다. 게스트 구독은 인증 없이 가능해 운영 자격 증명을 다루지 않는다.
- 주의: 가정용 네트워크와 Mac 성능이 수신 상한이 될 수 있다. k6 쪽 수신량·지연을 서버 지표와 함께 확인한다.

### D10·D11 구현 (2026-09-26)

- 모놀리식: `market.price-events.kafka-enabled`(기본 `true`)를 추가했다. 끄면 `MarketKafkaProducer`가 시세 이벤트를 Kafka로 보내지 않는다. 예약 주문 이벤트는 영향이 없다.
- k6 스크립트:
  - `WS_GUEST=true`이면 토큰 없이 연결 직후 구독한다. `setup()`의 토큰 발급도 건너뛴다.
  - `ws_e2e_lag_ms`: 모놀리식 틱 생성 시각(`eventTs`)부터 k6 수신까지의 지연을 기록한다. 구독 직후 스냅샷은 제외한다.
  - 프로파일 `ws-guest-1k`: 2분 동안 `WS_GUEST_VUS`(기본 1,000)까지 올린 뒤 `WS_HOLD`(기본 90분) 동안 유지한다.
- 스모크(5세션, 초당 250건·Kafka 켜짐): 연결 성공률 100%, 종단 지연 p50 29ms / p99 118ms.
  - 일부 음수 값은 Mac과 서버의 시계 차이(수십 ms) 때문이다. 종단 지연은 ±50ms 오차로 해석한다.

## 발견: 모놀리식 기동 지연의 원인은 활성 노드 수 조회용 전체 키 스캔 (2026-09-26 14:05~14:31)

- 증상: 새 모놀리식 파드(`8e15d9b`)가 부하(초당 250건) 중에 startupProbe 한도(10분)를 두 번 넘겨 재시작했다. 그동안 파드 CPU는 0.08코어 수준으로 낮았다.
- 로그(사용자 제공):
  - 05:17:43 UTC `ActiveNodeRegistry 초기화 완료`
  - 이어서 `KisCredentialAllocator.init()` → `ActiveNodeRegistry.getActiveNodeCount()`
  - `redissonClient.getKeys().getKeysByPattern("market:subscription-node:*")`가 `RedissonKeys` 반복자(클러스터 전체 `SCAN`) 안에서 8분간 진행되다가 종료 신호로 `InterruptedException`이 났다.
- 규모: Redis 키가 인스턴스당 약 68만 개(마스터 3대 합계 약 205만 개)다. Redisson 기본 `SCAN` COUNT(10)로 약 20만 번 왕복한다. 기동 중 `SCAN`이 초당 약 900번이므로 부하가 없어도 약 3~4분이고, Redis가 바쁘면 8분 이상 걸린다.
- 해석: 그동안 원인을 몰랐던 모놀리식 기동 290~350초의 대부분이 이 스캔으로 보인다. 활성 노드를 전용 집합(SET/ZSET)으로 관리하면 조회 한 번으로 끝난다.
- 처리: 이번 작업(#17) 범위에 넣을지는 사용자 결정으로 남긴다.

## D12. 활성 노드 수 조회 방식

- 결정일: 2026-09-26
- 검토한 선택지: 전용 목록(정렬 집합)으로 고치기 / 기동 한도만 20분으로 / 이전 버전으로 되돌리기
- 선택: **활성 노드를 정렬 집합 `market:subscription-nodes`(member = 노드 ID, score = 마지막 heartbeat)로 관리** (사용자 선택)

### 내용

- heartbeat마다 집합에 기록한다. 노드 수는 TTL(15초)이 지난 노드를 `ZREMRANGEBYSCORE`로 지운 뒤 `ZCARD`로 센다. 종료할 때 자신을 뺀다.
- 롤링 배포 동안 이전 버전 노드가 셀 수 있도록 노드별 키(`market:subscription-node:<id>`)도 계속 기록한다. 다음 배포에서 제거한다.
- 롤링 중에는 새 노드가 이전 노드를 세지 못해 잠시 1대로 판단할 수 있다. mock 시험 중이고 KIS 자격증명은 락으로 보호되므로 허용한다.

### 이유

- 기존 방식은 전체 키(약 205만 개)를 `SCAN`해서 기동에 3~8분이 걸렸다. 운영 중에도 5초(구독 동기화)·20초(자격증명 재조정)마다 같은 스캔을 반복했다.
- 효과 확인 지표: 모놀리식 기동 시간(`application_started_time_seconds`), Redis `SCAN` 명령 수, 모놀리식 CPU·스로틀링
- 고치기 전 기준(14:59, mock 정지·유휴): 이전 모놀리식 파드 CPU 0.60코어(스로틀링 38%), Redis `SCAN` 초당 1,525개(전체 명령 초당 3,832개의 약 40%, 기동 중인 새 파드 포함), 마스터 CPU 0.05~0.07코어

## 현재가 경로 1차 증량 측정 (D12 적용 후, Kafka 발행 끔, k6 게스트 1,000세션, 2026-09-26 15:19~15:32)

| 시각 | 목표 | 실제 발행 | 모놀리식 CPU / 스로틀링 | 틱 처리 p99 | 실행기 큐(가격/예약) | Redis 명령·CPU 최대 | 리스너 CPU 합·전달 | 노드 CPU |
|---|---|---|---|---|---|---|---|---|
| 15:24 | 250 | (전환 중) | 0.46 / 9% | 1ms | 0 / 0 | 4.9천/s · 0.17 | 0.13 · 294/s | 44% / 49% |
| 15:32 | 500 | **407** | **0.695 / 97%** | 87ms | 38 / 422 | 7.7천/s · 0.15 | 0.36 · 780/s | 43% / 58% |

- **현재가 경로의 첫 병목은 모놀리식 CPU 한도(0.7코어)다.** 초당 약 407건에서 멈춘다. Redis·리스너·노드는 여유가 크다.
- 틱당 모놀리식 CPU: D12 적용 전 약 3ms(0.7코어 ÷ 초당 약 220건) → 적용 후 약 1.7ms(0.7코어 ÷ 초당 약 407건). 같은 자원에서 현재가 처리량이 약 +85% 올랐다.

## D13. 모놀리식 현재가 병목 해소 방식

- 결정일: 2026-09-26
- 검토한 선택지: 틱당 CPU 프로파일링 후 코드 개선 / CPU 한도 상향 먼저 / 모놀리식 replicas 2
- 선택: **틱당 CPU 프로파일링 후 코드 개선** (사용자 선택)
- 방법: `JAVA_TOOL_OPTIONS`로 JFR(`settings=profile`)을 기동 5분 뒤부터 5분간 기록한다. 부하는 제어 키로 초당 400건에 고정한다. `jfr view hot-methods`로 CPU 상위 메서드를 뽑는다.

### D13 실행 결과 (2026-09-26 16:27~16:32, 초당 400건 고정, 실제 발행 약 345건/s)

- 진행 중 겪은 문제:
  - 처음에는 `JAVA_TOOL_OPTIONS`를 넣은 파드 안에서 `jfr` 도구를 실행했다. 도구의 JVM도 이 옵션을 읽어 같은 경로에 새 기록을 예약하면서 파일을 비웠다. 이후 도구를 실행할 때는 `env -u JAVA_TOOL_OPTIONS`를 붙였다.
  - 이미지가 JRE 전용(`eclipse-temurin:21-jre`)이라 `jcmd`로 실행 중인 JVM에 기록을 걸 수 없었다. 그래서 파드를 다시 띄워 처음부터 기록했다.
- 방법: `hot-methods`는 1위도 3.4%일 만큼 분산돼 판단이 어려웠다. 그래서 `jfr print --events jdk.ExecutionSample`의 호출 스택을 스레드별·라이브러리별·앱 프레임별로 집계했다(샘플 3,228개).

| 스레드 | 비중 | 내용 |
|---|---|---|
| `redisson-netty-*` | 29.6% | Redis 입출력 |
| `price-event-*` | 27.4% | 현재가 저장 Lua 15%, Pub/Sub 발행 10%(JSON 직렬화 포함) |
| `reservation-event-*` | 9.5% | 예약 조건 조회 `ZRANGEBYSCORE` 매수·매도 2회 |
| `market-scheduler` | 8.8% | 구독 동기화(락, 종목 DB 조회). 틱과 무관한 고정 비용 |
| `lettuce-nioEventLoop-*` | 6.2% | SPUBLISH 전용 Lettuce 연결 |
| mock 발행기 | 5.4% | 부하 발생기 |

- 라이브러리별(포함 기준): Netty 42%, Spring Data Redis 15%, Lettuce 11%, Jackson 9.5%, Micrometer 7.3%, Kafka 7%(발행을 꺼도 컨슈머가 돌아서 생김)
- **발견: `redisson-spring-boot-starter`가 Spring의 `RedisConnectionFactory`를 Redisson으로 바꿔 놓았다.** 그래서 `StringRedisTemplate`을 쓰는 모든 일반 명령(현재가 저장, 예약 조회 등)이 분산 락용 Redisson 클라이언트를 거친다. 틱당 Redis 왕복 4회 중 3회가 여기에 해당하고, 그 입출력 스레드가 CPU의 약 30%를 쓴다. 같은 일을 하는 Lettuce 연결(SPUBLISH)은 6%다.

## D14. 틱당 CPU 개선 대상

- 결정일: 2026-09-26
- 검토한 선택지: Redis 기본 연결을 Lettuce로 분리 / 예약 확인을 예약 있는 종목만 / 저장과 발행을 Lua 하나로 합치기 / 고정 비용(구독 동기화·메트릭) 제거
- 선택: **Redis 기본 연결을 Lettuce로 분리(`StringRedisTemplate`은 Lettuce, 분산 락만 Redisson)** (사용자 선택)

## D15. 모놀리식 CPU 한도

- 결정일: 2026-09-26
- 배경: 사용자가 CPU가 한도에 붙은 상태를 보고 모놀리식 CPU를 늘리기로 했다(D4 범위 안).
- 선택: **요청 1 / 한도 2코어**. 순서는 CPU 증설 → 새 한도에서 기준선 재측정 → D14 Lettuce 전환 → 같은 한도에서 전후 비교 (사용자 선택)
- 기존 값(요청 0.2 / 한도 0.7, 메모리 512Mi / 1Gi)은 매니페스트에 없었다. 클러스터에 직접 넣은 값이라 이번에 `resources`를 매니페스트에 명시했다(Manifest `1bccc1d`, JFR 옵션도 함께 제거).
- 노드 `vmi3244740` 기준: 요청 합계 4.46 → 5.26 / 6코어, 실사용 약 47%

## 발견: CPU 증설 후 롤링 배포 중 KIS 자격증명 쟁탈로 재시작 (2026-09-26 16:42~16:46)

- 한도를 2로 올린 새 파드가 종료 코드 1로 세 번 재시작했고, 네 번째에 떴다. OOM은 아니었다.
- 로그: `KIS credential allocation failed. No credentials assigned.` (`KisCredentialAllocator` 초기화 실패)
- 원인:
  - 자격증명 2개를 이전 파드가 모두 쥐고 있었다. 이전 파드는 약 20초마다 재조정할 때만 하나를 내놓는다.
  - 기동이 131초에서 58초로 빨라지면서, 새 파드가 그 재조정 전에 할당을 시도했다.
  - `kis.allocator.required=true`라서 할당에 실패하면 기동이 실패한다.
- 원래 있던 경쟁 조건이 빨라진 기동 때문에 드러난 것이다. 스스로 회복되고, mock 시험에는 영향이 없다. 고치는 방식은 나중에 결정 포인트로 다룬다.

## 현재가 경로 2차 증량 측정 (CPU 한도 2, Redisson 연결, Kafka 발행 끔, k6 게스트 1,000세션)

| 시각 | 목표 | 실제 발행 | 모놀리식 CPU / 스로틀링 | 틱 처리 p99 | 실행기 큐(가격/예약) | Redis 마스터 CPU 최대 · 명령 p99 | 리스너 CPU · 전달 | 노드 CPU |
|---|---|---|---|---|---|---|---|---|
| 16:51 | 400 | 389 | 0.63 / - | - | - | - | - | - |
| 16:54 | 800 | 782 (98%) | 1.17 / 5.7% | 1ms | 0 / 46 | 0.21 · 0.23ms | 0.48 · 1,255/s | 38% / 60% |
| 17:10 | 1,200 | 1,104~1,146 (92~95%) | 1.3~1.6 / 16~24% | 1~6ms | 43~233 / 117~491 | 0.25~0.27 · 0.25ms | 0.6~0.7 · 1.3~2.3천/s | 53~57% / 70~75% |
| 17:24~17:29 | 1,600 | **1,377 (86%, 5분 평균)** | **1.74 / 36% (5분 평균)** | 11~15ms | 최대 300 / 500(가득) | 0.30 · 0.25ms | 0.86 · 1.6~2.9천/s | 51~54% / 76~79% |

- **한도 2에서 한계는 약 초당 1,380건이다(0.7코어일 때 407건의 약 3.4배).** 1,200부터 순간 스로틀링과 실행기 큐 적체가 시작됐다. 1,600에서는 예약 실행기 큐가 가득 차(500) 호출 스레드가 직접 처리(CallerRuns)했고, CFS 스로틀링이 36%였다.
- Redis(마스터 CPU 최대 0.3, 명령 p99 0.25ms)와 리스너는 여유가 크다. 병목은 여전히 모놀리식 CPU다.
- 틱당 모놀리식 CPU는 약 1.3~1.5ms로 거의 그대로다. 처리량 증가는 자원을 늘린 덕분이다. 이 표가 D14(Lettuce 전환)의 비교 기준선이다.

## 발견: CPU 요청 1로는 무중단 롤링 배포가 스케줄되지 않음 (2026-09-26 17:31~17:46)

- D14 배포(`fa92165`)의 새 파드가 13분 동안 Pending이었다. 이벤트: `0/2 nodes are available: 1 Insufficient cpu, 1 node(s) had untolerated taint(s)`
- 원인:
  - `vmi2974623`(컨트롤 플레인)에는 taint가 있어 모놀리식을 올릴 수 없다.
  - `vmi3244740`의 CPU 요청 여유가 0.74코어뿐이다.
  - 배포 전략이 `maxUnavailable: 0`, `maxSurge: 1`이라, 이전 파드(요청 1)를 둔 채 새 파드(요청 1)를 먼저 띄우려다 자리가 없었다.
  - D15에서 요청을 1로 잡을 때 taint를 확인하지 않았다.
- 처리: 사용자 선택으로 **이번만 이전 파드를 수동 삭제**했다(모놀리식 약 1분 중단).
- 후속 결정(2026-09-26 18:57, 사용자 선택): 요청 0.5로 낮추기 / 먼저 내리고 올리기 / 매번 수동 삭제 중 **매번 이전 파드를 수동 삭제**한다. 새 파드가 Pending이 되면 `sudo kubectl -n finvibe delete pod <이전 파드>`를 실행한다.
- 재결정(19:57): 사용자가 "앞으로 계속 되게" 해 달라고 해서, **배포 전략을 `maxSurge: 0`, `maxUnavailable: 1`로 바꿨다**(Manifest `f4135ba`). CPU 요청·한도는 그대로다. 배포마다 약 1분 중단되지만 항상 스케줄되고, KIS 자격증명 쟁탈도 피한다.

## D14 효과 측정 (Lettuce 전환, CPU 한도 2, 1,600 목표, 구독 복구 후 17:54~17:59 5분 평균)

| 항목 | Redisson(기준선 17:24~17:29) | Lettuce | 변화 |
|---|---|---|---|
| 실제 발행 | 1,377/s | 1,340/s | 비슷함 |
| 모놀리식 CPU | 1.74코어 | 1.50코어 | -14% |
| 틱당 CPU | 약 1.26ms | 약 1.12ms | -11% |
| CFS 스로틀링 | 36% | 5% | CPU 병목 해소 |
| 예약 실행기 | 큐 가득(500) | 8/8 스레드 사용, 큐 평균 354 | 여전히 포화 |
| Redis 마스터 CPU 최대 · 서버 명령 p99 | 0.30 · 0.25ms | 0.33 · 0.23ms | 비슷함 |

- **처리량은 늘지 않았지만 병목의 성격이 바뀌었다.** CPU 스로틀링이 36%에서 5%로 줄어, 더 이상 CPU가 한계가 아니다.
- 새 한계는 예약 실행기다.
  - Lettuce 명령의 클라이언트 측정 평균 지연이 2.9ms다. Redis 서버 p99(0.23ms)보다 한참 크고, 명령은 초당 약 4,000개다.
  - 예약 확인은 스레드 8개가 동기로 틱마다 2회(매수·매도) 조회한다. 스레드 8개 ÷ (2회 × 2.9ms) ≈ 초당 1,380건으로, 실측 한계와 맞는다.
  - 큐가 차면 CallerRuns로 가격 실행기 스레드가 예약 확인까지 떠안아 전체가 느려진다.
- 틱당 CPU 절감은 기대(약 30%)보다 작은 11%였다. Redisson 입출력 부담이 Lettuce 입출력과 클라이언트 대기로 일부 옮겨 간 것으로 보인다.

## D16. 초당 1만 건 이상을 위한 방식

- 결정일: 2026-09-26
- 배경:
  - 사용자 목표는 초당 1만 건 이상이다. 예약 확인을 끄거나 없애는 것은 원하지 않는다("끄지마").
  - 병목 하나를 풀면 다음 병목이 나오는(CPU → 예약 스레드 → …) 방식이 아니라, 근본 대안을 요청했다.
- 검토한 선택지:
  - A. 모놀리식 안에서 묶음 처리로 재설계
  - B. 시세 수집을 별도 서비스(Go)로 분리
  - C. 지금 코드 그대로 모놀리식 수평 확장. 틱당 1.1ms라 약 11코어가 필요해서 불가능하다.
- 선택: **A. 모놀리식 안에서 묶음 처리 재설계** (사용자 선택). 세부 설계는 `02-plan.md`의 "D16 이후" 절에 있다.

## D17~D20. 묶음 처리 세부 결정 (2026-09-26, 모두 사용자 선택)

| 결정 | 검토한 선택지 | 선택 |
|---|---|---|
| D17. 버전 저장 전송 | 틱별 Lua를 파이프라인으로 / 슬롯별 다건 Lua | **틱별 Lua(`EVALSHA`)를 Lettuce 비동기 파이프라인으로** |
| D18. 메모리 예약 인덱스 동기화 | 변경 시 갱신 + 1초 재적재 / Pub/Sub 변경 알림 / 메모리 없이 묶음별 Redis 조회 | **Pub/Sub 변경 알림**(알림 유실 대비 재적재 포함) |
| D19. 리스너·프론트 | 배열 프레임 + 합치기 제거 / 배열 프레임, 합치기 유지 / 배열 수신만 | **배열 프레임 + 기존 합치기 제거**(틱 무손실) |
| D20. 부하 발생기 | 가짜 KIS 웹소켓 서버 파드 / 모놀리식 내 mock 유지 / Kafka 주입 | **가짜 KIS 웹소켓 서버 파드**(실제 입구 경로 포함) |

## 레인 묶음 처리 효과 측정 (모놀리식 `e4c9654`, 리스너 `f752073`, 1,600 목표, 구독 복구 후 20:08~20:13 5분 평균)

| 항목 | Redisson | Lettuce | 레인 묶음 |
|---|---|---|---|
| 실제 발행 | 1,377/s | 1,340/s | 1,340/s |
| 모놀리식 CPU | 1.74코어 | 1.50코어 | **1.12코어** |
| 틱당 CPU | 1.26ms | 1.12ms | **0.84ms** (Lettuce 대비 -25%, Redisson 대비 -34%) |
| CFS 스로틀링 | 36% | 5% | 0.8% |
| 예약 실행기 | 큐 가득 | 8/8, 큐 평균 354 | 7.6/8, 큐 평균 354 |
| 리스너 CPU(3파드 합) | 0.86 | 0.86 | 0.55 |
| Redis 마스터 CPU 최대 · 서버 명령 p99 | 0.30 · 0.25ms | 0.33 · 0.23ms | 0.26 · 0.16ms |

- 처리량은 예상대로 예약 실행기 한계(약 1,340~1,380/s)에 머물렀다. 비교는 틱당 CPU로 한다.
- 묶음 평균 10.9건, 실패 0건, 레인 큐 최대 6건, 오래된 틱 폐기는 거의 0이다.
- 리스너는 배열로 받으면서 Redis 메시지 수가 줄어 CPU가 약 36% 줄었다.
- 새로 보인 문제:
  - 묶음 처리 시간이 평균 9.2ms, p99 57ms다.
  - Lettuce 명령의 클라이언트 측정 평균이 2.7ms로, 서버 p99 0.16ms보다 훨씬 크다.
  - 클라이언트 쪽 대기(입출력 스레드 수, CPU 스케줄링 등)를 나중에 조사할 후보로 둔다.

## 발견: 구독 동기화의 KEYS가 Redis 마스터를 약 0.3초씩 멈춤 (2026-09-26 20:15)

- `KisSubscriptionSynchronizer`가 예약 종목을 찾으려고 `ReservationRepositoryImpl.findReservedStockIds()`로 `KEYS market:reservation:{buy,sell}:stock:*`를 보냈다.
- 운영 측정(5분):
  - 마스터 3대 각각 초당 0.021회(약 47초에 한 번) 실행된다.
  - 한 번에 약 0.29~0.31초 걸린다. 마스터당 키는 약 68만 개다.
  - Redis는 명령을 한 스레드로 처리하므로, 그동안 그 마스터의 모든 명령이 기다린다.
- 묶음 처리 p99(57ms)가 튀는 원인 후보다. 메모리 예약 인덱스를 만들면서 예약 종목 집합(`market:reservation:stocks`)으로 바꿨다.

## 메모리 예약 인덱스 효과 측정 (모놀리식 `3a414b8`, 1,600 목표, 구독 복구 후 20:29~20:32 3분 평균)

| 항목 | 레인 묶음(예약은 Redis 조회) | 레인 묶음 + 메모리 예약 인덱스 |
|---|---|---|
| 실제 발행 | 1,340/s (예약 실행기 한계) | **1,567/s (목표 대비 98%, 한계 아님)** |
| 모놀리식 CPU | 1.12코어 | **0.42코어** |
| 틱당 CPU | 0.84ms | **0.27ms** |
| 스로틀링 | 0.8% | 0% |
| 묶음 평균 크기 · 처리 시간 평균 / p99 | 10.9건 · 9.2ms / 57ms | 43건 · 15ms / 88ms |
| Redis 마스터 CPU 최대 · 서버 명령 p99 | 0.26 · 0.16ms | 0.14 · 0.12ms |
| 리스너 CPU · 전달 | 0.55 · 3.6천/s | 0.64 · 8.6천/s |

- 예약 실행기 한계가 사라졌다. 운영의 예약 종목은 0개라 틱마다 메모리 비교만 한다.
- 틱당 CPU는 처음(Redisson, 1.26ms)의 약 21%다. 2코어 기준 계산상 상한은 약 7천/s다.
- 21:35 재확인에서도 같은 수치다(1,571/s, 0.45코어).
- 발견: `KEYS`가 아직 3분에 3~5회 실행된다.
  - 원인은 `CurrentStockWatcherRepositoryImpl.findActiveStockIds()`의 `KEYS market:current-watcher:{stock:*}`다.
  - 구독 동기화와 오래된 현재가 복구가 이 조회를 쓴다.
  - 감시 키에 TTL이 있어 단순 집합으로는 대체할 수 없다(만료 시각 정렬 집합 등). 다음 개선 후보로 둔다.

## 현재가 경로 3차 증량 측정 (모놀리식 `3a414b8`, 한도 2, k6 게스트 1,000세션, 2026-09-26 21:53~22:04)

| 시각 | 목표 | 실제 발행 | 모놀리식 CPU / 스로틀링 | 묶음 평균 · 처리 평균 / p99 | 레인 큐 최대 | Redis 명령 · 마스터 CPU 최대 · p99 | 리스너 CPU · 전달 · 합치기(입구/출구) | 노드 CPU |
|---|---|---|---|---|---|---|---|---|
| 21:54 | 2,500 | 2,436 (1분) | 0.34 / 0% | 64건 · 14ms / 89ms | 115 | 2.9만/s · 0.20 · 0.11ms | 0.42 · 7.0천/s · 0/0 | 37% / 49% |
| 21:58 | 5,000 | 4,711 (94%) | 0.41~0.52 / 0% | 125건 · 40ms / 235ms | 250 | 5.0만/s · 0.25 · 0.10ms | 1.0 · 1.4만/s · 10/0.1 | 54% / 62% |
| 22:01~22:04 | 10,000 | **8,897 (89%, 3분)** | **0.60 / 0%** | 178건 · 52ms / 308ms | 1,668 | 9.0만/s · 0.29 · 0.09ms | 1.71 · 2.2만/s · 23/56 | 62% / 74% |

- **초당 약 9,000건에 도달했다.** 처음(0.7코어, 407건/s)의 약 22배이고, 모놀리식 CPU는 한도 2코어의 30%다.
- 모놀리식, Redis, 레인 모두 여유가 있다. 목표 대비 달성률이 94% → 89%로 떨어지는 것은 모놀리식 안 mock 발생기의 한계로 보인다(`dropped` 0, 발생기 틱 p99 1ms). D20(발생기 분리)으로 확인한다.
- 리스너 합치기가 시작됐다. 초당 약 80건으로 전달량의 약 0.4%다. D19(합치기 제거 + 배열 프레임)가 필요한 지점이다.
- 묶음 처리 p99가 약 300ms로 커졌다. 한 묶음 안 틱의 지연(체결 → 발행)이 그만큼 늘 수 있다.

## 20,000 목표에서 드러난 병목: Serial GC 정지 (2026-09-26 22:10~22:12)

- 증상:
  - 레인 큐 4개가 모두 가득 찼다(약 2만/2만).
  - 묶음이 최대 256건인데 처리 107ms, p99 575ms가 걸렸다.
  - 발행은 약 9,600~13,000/s에서 흔들렸고, 모놀리식 CPU는 0.67코어로 여유가 있었다.
- 원인: 메모리 한도 1Gi, CPU 2 조건에서 JVM이 **Serial GC**와 **힙 25%(260MB)**를 자동으로 골랐다.
  - 힙: Eden 72MB, Survivor 9MB, Old 179MB. 전체 GC 직후 Old에 남는 고정분이 약 100MB다(1,600 부하 때와 같다).
  - 할당 속도: 1,600건/s에서 17MB/s, 약 1만 건/s에서 95MB/s(틱당 약 9.5KB)다.
    - 틱 하나가 저장용·발행용 JSON 2개, 이벤트 객체, BigDecimal 7개, Lettuce 명령·인자·future를 만든다.
  - Eden이 약 0.75초마다 찬다. 그때 처리 중인 틱(큐 대기, Redis 응답 대기)이 Survivor를 넘쳐 Old로 밀려난다.
  - 그 결과 약 4초마다 전체 GC(MarkSweepCompact)가 일어나고, **최대 0.83초** 멈춘다. GC 정지는 전체 시간의 약 20%다.

## D21. GC 정지 해소

- 결정일: 2026-09-26
- 검토한 선택지:
  - G1 + 힙 75% + 메모리 1.5Gi
  - G1 + 힙 75%, 메모리 1Gi 유지
  - 틱당 객체 줄이기(코드)
  - 설정 먼저, 그다음 코드
- 선택: **G1 + 힙 75%(`-XX:+UseG1GC -XX:MaxRAMPercentage=75`) + 메모리 요청·한도 1.5Gi** (사용자 선택, Manifest `e79621f`)
- 노드 `vmi3244740`의 메모리 요청 합계는 약 6.7 → 7.7 / 11.7Gi다.
- 틱당 할당(9.5KB)을 줄이는 코드 개선은 이후 후보로 둔다.

## D21 효과와 다음 병목 (G1, 모놀리식 `3a414b8`, 2026-09-26 22:59~23:17)

| 항목 | Serial GC 10,000 | G1 10,000 (2분) | G1 20,000 (1분) |
|---|---|---|---|
| 실제 발행 | 8,897/s | 9,314/s | **10,681/s** |
| GC 정지 비율 · 최대 | 약 20% · 0.83초(전체 GC) | 1.3% · 0.087초 | 4.5% · 0.21초 |
| 모놀리식 CPU | 0.60 | 0.57 | 0.61 |
| 묶음 크기 · 평균 / p99 | 178건 · 52 / 308ms | 190건 · 56 / 337ms | 256건(최대) · 96 / 396ms |
| 레인 큐 | 최대 1,668 | 최대 500 | 1.4만~2만(가득) |
| 리스너 입구 합치기 | 23/s | 4,182/s | 5,345/s |

- GC 정지는 사라졌지만 묶음 처리 시간은 거의 그대로다. 묶음이 느린 주원인은 GC가 아니다.
- 20,000 목표에서 한계는 약 1.07만/s다. 레인 4개 × 256건 ÷ 0.096초와 같다. CPU(0.61/2)와 Redis(서버 p99 0.08ms)는 여유가 있어서, 레인은 계산이 아니라 무언가를 기다리고 있다.
- 리스너가 틱을 합치기(드롭) 시작했다. 입구 합치기가 초당 5천여 건이다. D19가 필요하다.

## D22. 레인 대기 원인 확인

- 결정일: 2026-09-26
- 검토한 선택지: 20,000에서 JFR로 원인 확정 / 레인 수 늘리기(4→16) / 레인마다 전용 Redis 연결 / 리스너(D19) 먼저
- 선택: **20,000에서 JFR로 원인 확정** (사용자 선택)
- 방법:
  - `JAVA_TOOL_OPTIONS`에 JFR을 추가했다(기동 14분 뒤부터 5분, Manifest `0d0a6e5`).
  - 부하는 1,000으로 낮춰 구독 재구축을 마친 뒤 20,000으로 올린다.
  - `ExecutionSample`과 대기 이벤트(`ThreadPark`, `JavaMonitorEnter`)를 스레드별·대기 위치별로 합산한다.

## 현재 진행 상태 (세션이 끊겨도 이어가기 위한 기록, 수시 갱신)

- 마지막 갱신: 2026-09-26 23:56
- 운영 상태:
  - 모놀리식 이미지 `3a414b8`(레인 묶음 + 메모리 예약 인덱스, 20:21 기동), 리스너 `f752073`, CPU 요청 1 / 한도 2, 배포 전략 maxSurge 0
  - mock 프로바이더, Kafka 시세 발행 꺼짐, 제어 키 `market:mock:control` = 10000(20,000으로 올릴 예정)
  - k6 웹소켓 게스트 1,000세션: 21:35에 다시 시작(150분)
- 코드 상태: D14 Lettuce 전환(`fa92165`)을 배포했다. 새 파드에서 `lettuce_seconds_count`가 초당 약 1,360건 잡혀 Lettuce 사용을 확인했다.
- 다음 순서:
  1. ~~제어 키를 1,600으로 올리고 5분 동안 기준선 한계를 기록한다.~~ 완료(약 초당 1,380건)
  2. ~~CI를 다시 실행해 `fa92165`를 배포한다.~~ 완료(스케줄 막힘 → 이전 파드 수동 삭제)
  3. ~~Lettuce 사용 확인과 1,600 전후 비교~~ 완료(위 D14 효과 측정).
  4. D16 결정: A안(묶음 처리 재설계). D17~D20도 결정했다. `02-plan.md` "진행 순서(안)" 2번을 구현했다.
     - 리스너 배열 수신 `f752073`: 배포 완료
     - 모놀리식 레인 묶음 처리 `4b78f76`(이미지 `e4c9654`): 배포·측정 완료(위 "레인 묶음 처리 효과 측정")
     - 3번 메모리 예약 인덱스(D18) `3a414b8`: 배포·측정 완료(위 "메모리 예약 인덱스 효과 측정")
     - 2,500 → 5,000 → 10,000 증량 완료(위 "3차 증량 측정", 약 9,000/s). 20,000에서 Serial GC 정지가 드러나 D21(G1·힙 75%·1.5Gi)을 배포했다(22:16 기동, G1 적용 확인). G1에서 10,000·20,000을 다시 쟀다(위 "D21 효과와 다음 병목"). 지금은 D22 JFR 기록을 위해 재배포했다(23:55). 부하 1,000 → 구독 완료 → 20,000 → 기록 → 집계 순서다. 그 뒤 4번 부하 발생기 분리와 5번 리스너 배열 프레임을 진행한다.
  5. 현재가 경로가 끝나면 수익률 부하 시험(Kafka 발행 다시 켜기)으로 넘어간다.
- 남은 정리:
  - `ActiveNodeRegistry`의 레거시 노드별 heartbeat 키 제거
  - 감시 종목 조회의 KEYS 제거(`CurrentStockWatcherRepositoryImpl.findActiveStockIds`)
  - KIS 자격증명 쟁탈 대응(배포 전략 변경으로 배포 중에는 발생하지 않음. 파드가 2대 이상일 때는 남는다)
  - mock 끄고 KIS 복구, `04-result.md` 작성, 레포별 PR
- 측정 절차 주의(2026-09-26 22:25):
  - mock은 구독된 종목만 돈다. 그래서 재배포 직후 구독 재구축 중에 높은 부하를 걸면 적은 종목에 틱이 몰린다. 이때 k6 세션 전달이 폭증했다(초당 5.5만 건, 노드 CPU 92%). 재구축도 분당 약 140종목으로 느려졌다.
  - 배포 전에 부하를 1,000 이하로 낮추고, 구독이 7,384종목까지 찬 뒤에 올린다.
- 운영 명령 형식(서버 `vmi2974623`에서): `sudo kubectl -n finvibe exec redis-cluster-1 -- sh -c 'redis-cli --no-auth-warning -a "$REDIS_PASSWORD" -c SET market:mock:control <값>'`
- JFR 주의: `JAVA_TOOL_OPTIONS`가 걸린 파드에서 `jfr` 도구를 쓸 때는 `env -u JAVA_TOOL_OPTIONS`를 붙인다. 이미지가 JRE 전용이라 `jcmd`는 없다.
