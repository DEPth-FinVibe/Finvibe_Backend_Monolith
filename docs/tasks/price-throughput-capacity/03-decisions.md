# 제한된 운영 자원에서 실시간 시세 처리량 한계 끌어올리기 Decisions

## D1. 단계별 측정 화면

- 결정일: 2026-09-26
- 검토한 선택지: 통합 대시보드 신규 / 기존 대시보드 + 문서 표 / 쿼리 결과만 문서에
- 선택: **Grafana에 "시세 처리량 부하 시험" 통합 대시보드를 새로 만들고, JSON을 Manifest 레포에 커밋** (사용자 선택)
- 이유: 단계별 전 구간을 한 화면에서 비교할 수 있고, 사용자가 직접 캡처해 포트폴리오 근거로 쓸 수 있다. JSON으로 재현할 수 있다.

## D2. 부하 발생 방식

- 결정일: 2026-09-26
- 검토한 선택지: mock 우선, 막히면 우회 추가 / mock만 / 처음부터 별도 발생 파드
- 선택: **모놀리식 mock으로 시작하고, 모놀리식이 병목이 된 뒤 뒤 단계 측정이 필요할 때만 별도 발생 파드를 추가** (사용자 선택)
- 이유: 실제 시세 진입점(버전 부여 포함)을 파이프라인에 그대로 두고 측정한다. 발생기 자체의 한계는 "목표 대비 실제 발행량"으로 구분한다.

## D3. 증량 단계·유지 시간·중단 기준

- 결정일: 2026-09-26
- 검토한 선택지: 세밀 단계 + 중단 기준 / 거친 단계 + 같은 기준 / 중단 기준 없이 끝까지
- 선택: **세밀 단계 + 중단 기준** (사용자 선택)

### 내용

- 단계(초당 건수): 100 → 250 → 500 → 1,000 → 2,500 → 5,000 → 10,000 → 20,000 → 37,000
- 유지 시간: 단계마다 5분
- 중단 기준(하나라도 해당하면 즉시 이전 단계로):
  - Redis 명령 지연 p99가 50ms를 넘는 상태가 1분 지속
  - 노드 CPU가 95%를 넘는 상태가 1분 지속
  - 파드 재시작 또는 OOM
  - 모놀리식 5xx 비율 1% 초과
- 모놀리식 재기동 없이 mock 설정을 바꾸는 방법을 S1에서 함께 검토한다(재기동마다 약 6분이 든다).

## D4. 자원 재배치 허용 범위

- 결정일: 2026-09-26
- 검토한 선택지: 노드 전체 안에서 자유 조정 / 한도 고정·코드 개선만 / 매번 개별 승인
- 선택: **노드 전체(2대 × 6코어) 안에서 파드 한도·replicas·배치를 자유롭게 조정** (사용자 선택)
- 적용 원칙: 코드 개선과 자원 조정의 효과를 구분하도록 한 번에 하나만 바꾸고, 변경마다 이 문서에 근거와 전후 수치를 남긴다.

### D1 적용 세부 (2026-09-26)

- 대시보드 UID는 `price-throughput-load-test`이다. Grafana API로 생성했다.
- JSON 위치: Manifest 레포의 `monitoring/`은 서비스 Argo CD 애플리케이션이 추적하는지 레포만으로 확인할 수 없었다. 그래서 대시보드 JSON이 매니페스트로 해석될 위험을 피해 **루트 `dashboards/`**에 두었다(사용자 선택. 검토안: 모놀리식 작업 문서 폴더 / Manifest 루트 `dashboards/` / Argo 설정 먼저 확인).
- 패널 구성: ① 부하(목표 대비 발행) ② 모놀리식 실행기·5xx ③ Redis 명령 처리량·상위 명령·지연·증폭률 ④ 워커 소비·반영 지연·미반영 ⑤ 리스너 전달·지연 ⑥ 파드·노드 CPU, 재시작
- 중단 기준 중 "Redis 명령 지연 p99"는 exporter가 명령별 평균 지연만 제공한다. 그래서 워커 쪽 Redis 명령 지연 히스토그램(`profit_worker_redis_command_duration_seconds`) p99로 판정한다.

## D5. mock 부하 조절 방식

- 결정일: 2026-09-26
- 검토한 선택지: 자동 증량 스케줄 + 긴급 정지 키 / Redis 키로 목표 부하 직접 조절 / 관리자 API / 재기동 방식 유지
- 선택: **자동 증량 스케줄 + 긴급 정지 키** (사용자 선택)

### 내용

- mock이 D3 단계표를 스스로 따라 올린다(단계마다 5분). 스케줄은 구독 종목이 처음 생긴 시점부터 시작해, 구독 재구축 시간이 첫 단계를 잡아먹지 않게 한다.
- 목표 부하와 현재 단계를 지표로 노출해 대시보드에서 "목표 대비 실제"를 비교한다.
- Redis 키 `market:mock:control` 하나로 제어한다. mock이 매 틱마다 읽는다.
  - `stop`: 발행 중지
  - `hold`: 현재 단계 고정
  - 숫자: 목표 부하 덮어쓰기
  - 키가 없으면: 스케줄대로 진행
- 이유: 재기동을 한 번만 하고 전 단계를 진행한다. 중단 기준에 걸리면 재기동 없이 즉시 멈출 수 있다.

## D6. 반복 중 운영 반영 방식

- 결정일: 2026-09-26
- 검토한 선택지: 반복마다 main 직접 push / 반복마다 PR 생성·병합
- 선택: **반복마다 작업 브랜치에서 커밋한 뒤 main에 직접 push해 배포·측정** (사용자 선택)
- 적용: 문서(overview~decisions)는 작업 브랜치에 계속 쌓는다. 마지막에 `04-result.md`와 PR을 "이미 반영된 변경의 기록"으로 작성한다.

### D3 정정: 중단 기준 "Redis 명령 지연 p99"의 측정 지표 (2026-09-26 01:26)

- D1 적용 세부에서 워커의 `profit_worker_redis_command_duration_seconds` p99를 중단 기준 지표로 삼았다. 그런데 초당 100건에서 이미 160ms가 나왔다. 확인해 보니 이 값은 명령 하나가 아니라 **명령 수백 개를 묶은 파이프라인 1회의 시간**이었다.
- Redis exporter가 명령별 서버 지연 히스토그램(`redis_commands_latencies_usec_bucket`)을 수집하고 있었다. 관리 명령(`INFO`, `LATENCY` 등)을 빼고 계산하면 서버 명령 p99는 55µs다. 가장 느린 명령은 `SADD` 3.7ms, `EVALSHA` 1.3ms다.
- D3의 기준값(50ms)은 유지하고, 판정 지표만 서버 명령 p99로 바꾼다. 대시보드 패널도 교체했다(워커 파이프라인 1회 시간은 참고선으로 남김).

## S2 1차 증량 측정 (2026-09-26 01:25~01:47)

증량 스케줄(D5)로 100 → 250 → 500 → 1,000 → 2,500(목표)까지 올린 뒤, 01:46에 `stop`으로 중단했다.

| 시각 | 목표 | 실제 발행 | 워커 소비 | 반영 지연 p50/p99 | 모놀리식 CPU | 실행기 큐 | Redis 명령·p99 | 노드 5.104.82.70 |
|---|---|---|---|---|---|---|---|---|
| 01:26 | 100 | 98 | — | —/2.0초 | 0.62/0.7 | — | 19.2만/s·— | 83% |
| 01:30 | 250 | 231 | **157** | 23초/59초 | 0.63 | 16 | 34.7만/s·47µs | **94%** |
| 01:40 | 1,000 | **349** | 158 | 252초/300초+ | 0.57 | **1000/1000** | 34.6만/s·47µs | 94% |
| 01:44 | 2,500 | 412 | 167 | 300초+/300초+ | 0.49 | 1000/1000 | 36.3만/s·47µs | 94% |

### 확인한 병목

1. **워커 처리량이 초당 약 157~169건에서 고정된다.** 워커 CPU는 1.1~1.4/4코어이고 Redis 서버 명령 p99는 47µs로 여유가 있다. 따라서 CPU나 Redis 서버가 아니라 워커 구조(동시성, 파이프라인 왕복, 명령 증폭)나 Redis 왕복 지연에서 막힌다. 발행이 이를 넘으면 반영 지연이 끝없이 늘어난다.
2. **mock 발생기가 초당 약 350~410건에서 포화된다.** `priceEventExecutor` 큐가 가득 차(1000/1000) CallerRuns로 발행 스레드가 막히고, 틱 처리 p99가 110~125ms가 된다. 모놀리식 CPU 한도는 0.7코어다.
3. **운영 Redis(v1 `redis-cluster-0~5`) 6개가 모두 노드 `vmi3244740`에 있다.** 모놀리식까지 같은 노드에 있어 노드 CPU가 94%다. 다른 노드는 44~55%다.

### 참고: redis-cluster-v2

- 사용자가 `redis-cluster-v2`를 두 노드에 분산(4:2)했다. 하지만 운영 앱은 모두 v1(`redis-cluster-*.redis-cluster-headless`)에 연결되어 있다.
- 파드별 CPU(v1 0.26~0.64코어, v2 0.01~0.03코어)와 수신 트래픽(v1 3.3~9.2MB/s, v2 0.01MB/s)으로 v2에 트래픽이 없음을 확인했다. 그래서 이번 부하에는 영향이 없었다.

## D7. 첫 개선 대상

- 결정일: 2026-09-26
- 검토한 선택지: v1 Redis 노드 분산 / 워커 처리량 먼저 / 모놀리식·발생기 먼저
- 선택: **운영 v1 Redis(`redis-cluster`)를 두 노드에 분산** (사용자 선택)
- 이유: 같은 노드의 CPU 경합(94%)이 워커 지연의 원인일 수 있다. 이것을 먼저 없애야 워커 개선 효과를 정확히 잴 수 있다. 마스터·레플리카가 한 노드에 있는 가용성 문제도 함께 해소한다.

### 확인한 제약

- 스테이트풀셋에는 이미 선호형 분산 설정(preferred anti-affinity, `whenUnsatisfiable: ScheduleAnyway`)이 있다. 강제력이 없어 6개가 한 노드에 몰렸다.
- `nodes.conf`와 데이터가 노드에 묶인 PVC(`/data`)에 있다. 파드를 옮기려면 그 파드의 PVC를 지워야 한다. 이때 인스턴스는 새 노드 ID로 빈 채 시작하므로 `CLUSTER FORGET`과 `--cluster add-node --cluster-slave`로 다시 붙여야 한다.
- 매니페스트에서 anti-affinity를 강제형으로 바꾸면 PVC 노드 고정과 충돌해 파드가 `Pending`에 빠질 수 있다. 이 방법은 쓰지 않는다.
