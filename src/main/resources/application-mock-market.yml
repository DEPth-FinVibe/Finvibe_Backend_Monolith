market:
  provider: mock
  mock:
    emit-interval-ms: 1000  # 스케줄러 발화 주기 (ms). 낮출수록 이벤트 빈도 증가
    stocks-per-tick: 0       # 틱당 이벤트 발행 종목 수. 0 = 전체 구독 종목
    publish-threads: 0       # 병렬 발행 스레드 수. 0 = CPU 코어 수
